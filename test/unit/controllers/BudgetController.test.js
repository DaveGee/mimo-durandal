var request = require('supertest');

describe('BudgetController', function() {

    /*describe('#getOrCreate()', function() {
        it('should (create and) get new budget', function (done) {
            request(sails.hooks.http.app)
                .get('/Budget/dge/2015')
                //.send({ name: 'test', password: 'test' })
                .expect(200)
                .expect(function(res) {
                    console.log(res);
                    if(!res.body ||
                        res.body.year !== 2015 || res.body.owner !== 'dge') {
                        throw "Error!"
                    }
                })
                .end(done);
        });
    });*/

});